/*
Завдання 1: Сортування бульбашкою (Bubble Sort)
Концепція: Найпростіший, але найменш ефективний алгоритм. Його суть у тому, що найбільші елементи «спливають» у кінець масиву, як бульбашки повітря у воді.
Опис алгоритму:
1. Порівнюємо два сусідні елементи: a[i] та a[i + 1].
2. Якщо лівий елемент більший за правий — міняємо їх місцями.
3. Повторюємо це для кожної пари сусідів до кінця масиву.
4. Після одного повного проходу найбільше число гарантовано опиниться в кінці.
5. Повторюємо прохід для частини масиву, що залишилася (без останнього елемента).
Технічне Завдання для реалізації:
• Створити функцію bubbleSort(arr).
• Використати вкладені цикли for.
• Додаткове завдання*: Додати оптимізацію (переривання циклу, якщо за цілий прохід не було зроблено жодної заміни).
*/

arr = [32, 254, 13, 66, 367, 1, 9];

function bubbleSort(arr) {

    for (let i = 0; i < arr.length; i++) {
        let swapped = false;
        for (let index = 0; index < arr.length - 1; index++) {
            if (arr[index] > arr[index + 1]) {
                let inne = arr[index];
                arr[index] = arr[index + 1];
                arr[index + 1] = inne;
                swapped = true;
            }
        }
        if (!swapped) {
            break;
        }
    }
    return arr;
}

console.log(bubbleSort(arr));



/*
Завдання 2: Сортування вибором (Selection Sort)
Концепція: Щоразу шукається найменший елемент у невідсортованій частині масиву і ставиться його на початок.
Опис алгоритму:
1. Припускається, що перший елемент — мінімальний.
2. Відбувається прохід по решті масиву, щоб знайти фактичний мінімум.
3. Якщо знайдено число, менше за поточний мінімум — запам'ятовується його індекс.
4. Після завершення проходу міняються місцями знайдений мінімум з елементом на першій позиції.
5. Відбувається повторення кроків 1-4 для наступної позиції (другої, третьої і т.д.).
Технічні Завдання для реалізації:
• Створити функцію selectionSort(arr).
• Ввести змінну minIndex для відстеження найменшого значення під час кожної ітерації.
*/

arr = [32, 254, 13, 66, 367, 1, 9];

function selectionSort(arr) {
    for (let i = 0; i < arr.length; i++) {
        let minIndex = i;
        for (let j = i + 1; j < arr.length; j++) {
            if (arr[j] < arr[minIndex]) {
                minIndex = j;
            }
        }
        let inne = arr[i];
        arr[i] = arr[minIndex];
        arr[minIndex] = inne;
    }
    return arr;
}

console.log(selectionSort(arr)


/*
Завдання 3: Сортування вставками (Insertion Sort)
Концепція: Нагадує перемішування колоди карт. Береться один елемент і "вставляється" його на правильне місце серед уже відсортованих ліворуч від нього.
Опис алгоритму:
1. Починається з другого елемента (вважається, що перший уже "відсортований").
2. Відбувається порівняння поточного елементу із тими, що стоять ліворуч.
3. "Зсуваємо" всі елементи, які більші за поточний, на одну позицію праворуч.
4. Вставляємо поточний елемент на вільне місце, що звільнилося.
Технічне Завдання для реалізації:
• Створити функцію insertionSort(arr).
• Використати цикл while всередині циклу for для зміщення елементів ліворуч.
*/


